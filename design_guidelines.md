# 印谱 — 设计指南

## 品牌定位
- **应用定位**：自用的篆刻预览工具（草稿 → 印章 → 宣纸钤印）
- **设计风格**：东方克制 / 印谱气质 / 留白为美
- **目标用户**：篆刻爱好者本人（单用户单场景）

## 配色方案（必须使用以下色值）
- **朱砂红** `#C0392B` — 主色，印章与关键按钮
- **宣纸米白** `#F4EFE6` — 页面背景
- **白宣** `#FBF7F0` — 卡片底色（比背景略白一层）
- **徽墨深灰** `#2C2A28` — 标题与正文
- **淡墨灰** `#7A7570` — 辅助说明文字
- **浅米黄** `#E5DCC9` — 描边、分割线

## 字体规范
- 中文：`Noto Serif SC`（思源宋体）
- 西文/数字：`Noto Serif`
- 标题 32px / 副标题 18px / 题款 14px / 正文 15px
- 行高 1.7-1.8

## 间距系统
- 页面左右内边距：`p-4` / `px-4`
- 卡片内边距：`p-4` 或 `p-6`
- 区块间距：`gap-4` / `space-y-4`
- 印章图与文字距离：`gap-3`

## 组件使用原则
- **按钮**：必须用 `@/components/ui/button`；主操作 `variant="default" bg-[#C0392B]`；次要 `variant="outline"`
- **卡片**：用 `@/components/ui/card`，圆角 `rounded-lg`（≤8px），不模糊超过 8px
- **图片展示**：用 `@/components/ui/card` 包裹 `Image`，加 `bg-[#FBF7F0]` 模拟册页
- **进度展示**：用 `@/components/ui/skeleton` 占位（不显示百分比，用文案节奏）
- **步骤标签**：用 `@/components/ui/badge`，颜色用朱砂红
- **禁止**：用 `View/Text` + Tailwind 手搓按钮/卡片/标签/输入框等通用组件
- **禁止**：emoji、彩色按钮、弹窗、阴影模糊>8px、圆角>8px、渐变背景

## 页面结构（单页：首页 = 全部流程）
1. **顶部标题区**：产品名"印谱" + 一句副标题"草稿落石前，先看看盖出来的样子"
2. **上传区**：未上传时显示"点击/拖拽上传印章草稿"的引导卡片（米白底+朱砂红描边）
3. **处理进度区**：上传后显示三步进度（草稿→刻章→钤印），用 Badge + Skeleton
4. **结果对比区**：分三个阶段展示
   - 草稿（原图）
   - 印章图（朱砂红底白字）
   - 宣纸效果（最终图）
5. **操作区**：保存到相册 + 重新开始

## 导航结构
- 单页面应用，无 TabBar
- 无需历史记录/列表/设置页

## 小程序约束
- 印章图必须用 TOS 对象存储 URL，不打包到项目
- 处理进度文案："正在刻章…" / "正在钤印…" / "完成"
- 跨端：微信小程序为主，H5 作为预览兜底
